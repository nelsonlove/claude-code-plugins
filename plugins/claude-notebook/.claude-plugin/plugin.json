{
  "name": "claude-notebook",
  "version": "0.6.0",
  "description": "Save the current Claude Code session as a timestamped section appended to today's daily agent note in the JD vault's 03.13 Agent notebook/. Sessions accumulate as `## HH:MM — <headline>` sections within `Agent note for YYYY-MM-DD.md`."
}
